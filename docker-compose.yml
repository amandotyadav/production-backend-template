services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    image: production-backend:latest

    container_name: production-backend

    ports:
      - "8080:8080"

    env_file:
      - .env

    restart: unless-stopped
